{% extends "layout.njk" %}
{% block content %}
<main class="container">
  {% if part %}
  <div>
    <h1>{{part.name}}</h1
    {% if part.image %}>
    <img src="/img/{{part.image}}" alt="{{part.name}}">
    {% endif %}
    <p>{{part.text}}</p>
    <ul>
      {% for option in part.options %}
      <li style="list-style: none; text-emphasis: none;"><a href="/story/{{option.target}}">{{option.text}}</a></li>
      {% endfor %}
    </ul>
  </div>
  {% else %}
  <p>There is no part to display</p>
  {% endif %}
</main>
{% endblock %}